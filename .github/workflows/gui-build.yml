name: gui-build

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "gui/**"
      - "CMakeLists.txt"
      - ".github/workflows/gui-build.yml"

permissions:
  contents: read

jobs:
  ubuntu-2404-gui-build:
    name: Ubuntu 24.04 Qt GUI build
    runs-on: ubuntu-24.04
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check documentation
        run: |
          ./scripts/check-docs.sh

      - name: Install GUI build dependencies
        timeout-minutes: 25
        run: |
          set -euxo pipefail

          sudo tee /etc/apt/sources.list.d/ubuntu.sources >/dev/null <<'APT_EOF'
          Types: deb
          URIs: http://archive.ubuntu.com/ubuntu/
          Suites: noble noble-updates noble-backports
          Components: main restricted universe multiverse
          Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

          Types: deb
          URIs: http://security.ubuntu.com/ubuntu/
          Suites: noble-security
          Components: main restricted universe multiverse
          Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
          APT_EOF

          sudo apt-get -o Acquire::Retries=5 update

          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            pkg-config \
            file \
            libopencv-core-dev \
            libopencv-videoio-dev \
            libpam0g-dev \
            libsodium-dev \
            qt6-base-dev

      - name: Configure GUI build
        run: |
          cmake -S . -B build-gui -DBUILD_GUI=ON

      - name: Build GUI
        run: |
          cmake --build build-gui

      - name: Verify GUI artifact
        run: |
          test -x build-gui/gui/face-unlock-enroll
          file build-gui/gui/face-unlock-enroll

      - name: Upload GUI artifact
        uses: actions/upload-artifact@v4
        with:
          name: face-unlock-enroll-ubuntu-24.04
          path: build-gui/gui/face-unlock-enroll
