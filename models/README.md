# Models

This directory documents model artifacts used by the daemon.

Large model files are intentionally not committed to Git.

Ignored examples:

- `.pt`
- `.onnx`
- `.engine`
- TensorRT engines

Model files should be downloaded, exported, or generated locally.
